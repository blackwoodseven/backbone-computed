var computed = {};

// Computed mixin
// overrides set and get method
computed.mixin = {
    get: function(attr) {
        //.. do nothing
    },

    set: function(attr, value, options) {
        // .. do nothing
    }
}